# Password for the gated site (read by src/middleware.ts in production).
# If unset, the gate fails closed and nobody can get in.
# Copy this file to .env locally, or set it on Vercel in
# Project Settings → Environment Variables. `astro dev` skips the gate.
SITE_PASSWORD=
