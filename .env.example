# Optional override for the gated /commission case study password.
# If unset, the default password in src/middleware.ts is used.
# To override: copy this file to .env locally, or set it on Vercel in
# Project Settings → Environment Variables.
COMMISSION_PASSWORD=
